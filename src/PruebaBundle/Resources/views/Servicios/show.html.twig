{% extends 'base.html.twig' %}

{% block body %}


    <h1>Servicio</h1>
      <div class="table-responsive ">
			<table class="table table-sm  mb-3" style="width: 35%" >
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ servicio.id }}</td>
            </tr>
            <tr>
                <th>Numero de referencia</th>
                <td>{{ servicio.referencia }}</td>
            </tr>
            <tr>
                <th>Compañia</th>
                <td>{{ servicio.compania}}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ servicio.direccion}}</td>
            </tr>
            <tr>
                <th>Ciudad</th>
                <td>{{ servicio.ciudad}}</td>
            </tr>
            <tr>
                <th>Tipo de servicio</th>
                <td>{{ servicio.tipo}}</td>
            </tr>
            <tr>
            <tr>
                <th>Num telefono</th>
                <td>{{ servicio.numTelefono}}</td>
            </tr>
            </tr>
        </tbody>
    </table>

    </div>


<div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
            <a href="{{ path('servicio_edit', { 'id': servicio.id }) }}" class="btn btn-outline-info">Editar </a>

            <a class="btn btn-outline-info"></class> {{ form_start(delete_form) }} Eliminar
                {{ form_end(delete_form) }} </a>

            <a href="{{ path('servicio_index') }}" class="btn btn-outline-info" role="button" aria-pressed="true">Volver al listado</a>
        
             <a href="{{ path('prueba_homepage') }}" class="btn btn-outline-info" role="button" aria-pressed="true">Volver al menú principal</a>
        </div>

    <br>
        
</ul>


{% endblock %}
