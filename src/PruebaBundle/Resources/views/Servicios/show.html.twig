{% extends 'base.html.twig' %}

{% block body %}

<ul>
    <h1>Servicio</h1>
    <table class="table table-borderless">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ servicio.id }}</td>
            </tr>
            <tr>
                <th>Numero de referencia</th>
                <td>{{ servicio.referencia }}</td>
            </tr>
            <tr>
                <th>Compañia</th>
                <td>{{ servicio.compania}}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ servicio.direccion}}</td>
            </tr>
            <tr>
                <th>Ciudad</th>
                <td>{{ servicio.ciudad}}</td>
            </tr>
            <tr>
                <th>Tipo de servicio</th>
                <td>{{ servicio.tipo}}</td>
            </tr>
            <tr>
            <tr>
                <th>Num telefono</th>
                <td>{{ servicio.numTelefono}}</td>
            </tr>
            </tr>
        </tbody>
    </table>

    <ul>
    <br>
            <a href="{{ path('servicio_index') }}">Volver al listado</a>
            <br>
            <a href="{{ path('prueba_homepage') }}">Volver al menú principal</a>
    </ul>
    <br>
        <div class="btn-group" role="group" aria-label="Basic example">
            <a href="{{ path('servicio_edit', { 'id': servicio.id }) }}" class="btn btn-outline-primary">Editar </a>

            <a class="btn btn-secondary"></class> {{ form_start(delete_form) }} Eliminar
                {{ form_end(delete_form) }} </a>
        </div>
</ul>


{% endblock %}
