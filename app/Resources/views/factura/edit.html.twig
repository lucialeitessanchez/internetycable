{% extends 'base.html.twig' %}

{% block body %}
   <ul>
    <h1>Editar Factura</h1>

       <div class="mb-3 row"><label for="staticEmail" class="col-sm-2 col-form-label">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
       </label>
       </div>

       <div class="btn-group" role="group" aria-label="Basic example">
           <a href="{{ path('factura_edit', { 'id': factura.id }) }}" class="btn btn-secondary"> Editar </a>
           {{ form_end(edit_form) }}

           {{ form_start(delete_form) }}
           <a href="{{ path('factura_index') }}" class="btn btn-outline-primary">Eliminar</a>
           {{ form_end(delete_form) }}
       </div>
    <ul>

            <a href="{{ path('factura_index') }}">Volver al listado</a>
            <br>
            <a href="{{ path('prueba_homepage') }}">Volver al menú principal</a>



    </ul>
    </ul>
{% endblock %}
