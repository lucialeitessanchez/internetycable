{% extends 'base.html.twig' %}

{% block body %}
    <ul>
    <h1>Creación de Factura </h1>

        <div class="mb-3 row"><label for="staticEmail" class="col-sm-2 col-form-label">
                {{ form_start(form) }}
                {{ form_widget(form) }}
            </label>
        </div>
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <br>
            <a href="{{ path('factura_index') }}">Volver al listado</a>
            <br>
            <a href="{{ path('prueba_homepage') }}">Volver al menú principal</a>

    </ul></ul>
{% endblock %}
