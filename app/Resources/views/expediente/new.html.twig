{% extends 'base.html.twig' %}

{% block body %}
    <ul>

    <h1>Expediente nuevo</h1>

        <div class="mb-3 row"><label for="staticEmail" class="col-sm-2 col-form-label">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            </label>
        </div>

        <br>

     <th><div class="btn-group" role="group" aria-label="Basic example">
        <input type="submit" class="btn btn-secondary" value="Crear" />
    {{ form_end(form) }}

  
                        <a href="{{ path('expediente_index') }}" class="btn btn-primary">Volver al listado</a>
                        <a href="{{ path('prueba_homepage') }}" class="btn btn-secondary"> Volver al menú principal </a>

                    </div>
                </th>

    </ul>


{% endblock %}
