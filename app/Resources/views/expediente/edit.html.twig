{% extends 'base.html.twig' %}

{% block body %}
   <ul>
    <h1>Editar Expediente</h1>
       <br>
       <div class="mb-3 row"><label for="staticEmail" class="col-sm-2 col-form-label">
            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            </label>
        </div>
        <br><br>
<th>
<div class="btn-group" role="group" aria-label="Basic example">
  <input type="submit"  class="btn btn-outline-info" value="Editar" />
            {{ form_end(edit_form) }}
            {{ form_start(delete_form) }}
   <input type="submit"  class="btn btn-outline-info" value="Eliminar">
            {{ form_end(delete_form) }}
  <a href="{{ path('expediente_index') }}"  class="btn btn-outline-info">Volver al listado</a>
  <a href="{{ path('prueba_homepage') }}"  class="btn btn-outline-info">Volver al menú principal</a>
</div>
</th>
        
    </ul>
{% endblock %}
