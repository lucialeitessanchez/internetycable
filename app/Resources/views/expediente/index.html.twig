{% extends 'base.html.twig' %}

{% block body %}
    <h1>Expedientes</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero de expediente</th>
                <th>Estado</th>

            </tr>
        </thead>
        <tbody>
        {% for expediente in expedientes %}
            <tr>
                <td><a href="{{ path('expediente_show', { 'id': expediente.id }) }}">{{ expediente.id }}</a></td>
                <td>{{ expediente.numeroExpe }}</td>
                <td>{{ expediente.estado }}</td>
                <td>
                    <ul>
                        <button type="button" class="btn btn-primary btn-sm"><a href="{{ path('expediente_show', { 'id': expediente.id }) }}">ver</a></button>

                        <button type="button" class="btn btn-secondary btn-sm"><a href="{{ path('expediente_edit', { 'id': expediente.id }) }}">editar</a></button>



                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>

            <a href="{{ path('expediente_new') }}">Crear nuevo expediente</a>
            <a href="{{ path('prueba_homepage') }}">Volver al menú principal</a>

    </ul>
{% endblock %}
